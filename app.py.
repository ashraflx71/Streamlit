import streamlit as st

# إعدادات الصفحة
st.set_page_config(page_title="تطبيقي الأخضر", page_icon="🌿")

# عنوان التطبيق
st.title("مرحباً بك في تطبيقي الأول 🚀")

# نص بسيط
st.write("هذا التطبيق تم إنشاؤه ليكون صديقاً للبيئة ومبرمجاً بذكاء.")

# إضافة تفاعل بسيط
name = st.text_input("ما اسمك؟")
if name:
    st.success(f"أهلاً بك يا {name}! نحن نبني مستقبلاً أخضر معاً.")
